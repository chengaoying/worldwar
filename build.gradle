ext.jmeArchivesBaseName = 'worldwar'

configurations {
    refProvide
    compile.extendsFrom(refProvide)
}

repositories {
    maven {
        credentials {
            username 'deployment'
            password '123456'
        }
        url 'http://192.168.16.70:8000/nexus/content/groups/public/'
    }
}

dependencies {
    refProvide 'ohyeah:itvgame-common-dev:1.0.1'
}

apply from: "${System.getenv().IGBT_HOME}/build.gradle"